
var Git = require('nodegit');

// https://github.com/nodegit/nodegit/blob/master/test/tests/branch.js


// name, commit, force, signature, logMessage
repo.createBranch(name, masterCommit, true);
repo.createBranch(name, commit)


repo.getBranchCommit(name)

repo.checkoutBranch(name)

repo.getCurrentBranch()

repo.getBranch(branchName)

Git.Branch.delete(name)

Git.Branch.setUpstream(branch, upstreamName)

Git.Branch.upstream(branch)
.then(function(upstream) {
assert.equal(upstream.shorthand(), upstreamName);
})
